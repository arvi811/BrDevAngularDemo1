import { AppRedElDirective } from './app-red-el.directive';

describe('AppRedElDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRedElDirective();
    expect(directive).toBeTruthy();
  });
});
